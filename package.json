{
  "name": "algo-training",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "find . -name '*.js' | grep -v .git | grep -v node_modules | xargs mocha --compilers js:babel-core/register",
    "live": "find . -name '*.js' | grep -v .git | grep -v node_modules | entr -r npm run test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.20.0",
    "babel-preset-es2015": "^6.18.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
